# Example environment variables for Mountain of Many Voices
# Copy this to .env and fill in your values

# API key for DashScope (required for AI generation)
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# Port for the server (defaults to 3001 if not set)
PORT=3001

# Set to any value to enable production mode
# NODE_ENV=production